# MathModel-DE-Skill

面向全国大学生数学建模竞赛（CUMCM）D/E题的专用建模 Skill。

## 核心目标

将 D/E 题处理从“套模型”升级为：

> 题型识别 → 数据/机理分析 → 历题相似检索 → 候选模型 → 模型比较 → 主模型确定 → Python求解 → 验证与稳健性 → 图表 → 论文表达

## 总原则

1. **先理解问题，再选择模型。** 不因模型高级而使用模型。
2. **优先建立可解释、可验证的基线模型。** 再考虑复杂模型。
3. 每个核心模型至少说明：适用条件、假设、变量、目标函数/方程、参数来源、求解方法、验证方法、局限性。
4. 不得虚构数据、参数、实验结果、文献或竞赛结论。
5. 所有数值结论必须能够追溯到题目附件、计算过程或明确的数据处理步骤。
6. 对多个候选模型进行比较时，应说明评价指标和选择理由。
7. 论文中的图表必须来自真实计算结果；不得为了“好看”伪造趋势。
8. 复杂算法不是默认答案。只有在数据规模、非线性、组合爆炸、预测精度或优化难度确有需要时才升级算法。

## D题重点能力

- 机理建模
- 概率统计与不确定性
- 优化与多目标优化
- 动力/传播/扩散/仿真
- 网络、路径与调度
- 参数敏感性与稳健性
- 数据驱动模型与机理模型融合

典型思路：**机理模型 → 参数估计 → 仿真/优化 → 敏感性/稳健性分析**。

## E题重点能力

- 数据清洗与探索性分析
- 指标构建与特征工程
- 回归与分类
- 时间序列预测
- 聚类与降维
- 综合评价
- 机器学习
- 决策与优化
- 可视化与数据解释

典型思路：**数据理解 → 数据质量 → 特征/指标 → 基线模型 → 候选模型比较 → 预测/评价 → 决策优化**。

## 模型选择约束

优先回答以下问题：

- 这是预测、评价、优化、分类、聚类、仿真还是机理解释问题？
- 因变量/目标变量是什么？
- 数据量是否支持复杂模型？
- 是否存在明显时间、空间、网络或物理约束？
- 是否需要可解释性？
- 是否存在训练集/测试集或交叉验证需求？
- 是否需要约束优化、整数决策或多目标权衡？

## 标准输出

处理一套完整 D/E 题时，原则上形成：

- `problem_analysis.md`：问题拆解、变量、假设、数据质量与题型判断
- `model_selection.md`：候选模型、优缺点、最终选择
- `code/`：可重复运行的 Python 代码
- `results/`：原始计算结果与中间结果
- `figures/`：数据驱动图表
- `verification.md`：误差、敏感性、稳健性与合理性检验
- `paper/`：论文各章节素材

## 推荐执行顺序

### Step 1：题目体检
提取背景、目标、约束、输入数据、输出要求、隐含变量。

### Step 2：附件体检
检查表头、单位、缺失值、异常值、重复值、时间粒度、样本量、变量类型。

### Step 3：题型识别
判断每一问属于预测/评价/优化/仿真/统计/分类/机理建模等类型，并允许一题多模型组合。

### Step 4：历史题相似检索
检索 D/E 历年题及公开优秀论文的“问题结构”和“方法结构”，而不是机械复制答案。

### Step 5：模型候选
至少给出一个可解释基线模型；复杂模型必须有升级理由。

### Step 6：建模与求解
明确变量、参数、假设、方程/目标函数、算法和终止条件。

### Step 7：验证
至少进行数量级/边界/残差/预测误差/敏感性/稳健性中的适用项检查。

### Step 8：论文表达
做到“结论来自计算、图表来自数据、模型来自问题”。

## 禁止事项

- 禁止直接套用历年论文而不解释适用性。
- 禁止用虚构数据填补关键结果。
- 禁止只报告训练集指标。
- 禁止将相关性直接表述为因果关系。
- 禁止把模型输出当作现实规律而不做合理性检验。
- 禁止为了增加模型数量而堆叠无意义算法。
