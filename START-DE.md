# START-DE：国赛 D/E 总控入口

当用户给出一道全国大学生数学建模竞赛 D/E 题及附件时，按本流程执行。

## 0. 启动

读取 `SKILL.md`，确认比赛年份、题号、题面和附件。不得在附件未读取时假设数据结构。

## 1. 全题审计

输出：

- `problem_analysis.md`
- `data_audit.md`
- `todo.md`

必须识别：每一问目标、输入、输出、变量、单位、约束、数据来源、评价指标、跨问依赖。

## 2. D/E 与结构识别

调用：

- `01_problem_analysis/D题识别.md`
- `01_problem_analysis/E题识别.md`
- `02_model_selection/模型选择决策树.md`

如果 D/E 特征混合，不强行二选一，按数学结构判断。

## 3. 历题匹配

优先检索 `benchmarks/`：

- 2024D
- 2024E
- 2025D
- 2025E

匹配依据不是关键词，而是：对象、数学结构、数据结构、约束结构、决策结构、验证方式。

输出：`similarity_report.md`。

## 4. 候选模型

至少提出：

- 一个可解释 baseline
- 一个主候选模型
- 一个必要时的升级/备选模型

调用 `02_model_selection/D_E模型推荐评分.md` 打分。除非存在明确证据，不得因为“高级”而优先选择复杂模型。

## 5. 逐问求解

每一问按照：

```text
问题 → 假设 → 变量 → 模型 → 求解 → 结果 → 验证 → 结论
```

先完成当前问并冻结结果，再进入下一问。

## 6. Python

遵循 `05_python/README.md`：

- 原始数据只读
- 固定随机种子
- 保存中间结果
- 保存最终结果
- 真实运行
- 记录运行参数

## 7. 验证

至少选择与模型匹配的验证：

- baseline 对照
- 残差/误差诊断
- 交叉验证或时间留出
- 参数敏感性
- 扰动稳健性
- Monte Carlo 重复
- 约束可行性
- 极端情景

不能为了“通过验证”而选择有利数据。

## 8. 图表

调用 `06_visualization/论文级图表规范.md`。

所有结果图来自真实运行结果；示意图和数据图必须明确区分。

## 9. 论文

调用 `07_paper/论文写作流水线.md`。

论文只能使用冻结后的结果。任何数字都必须可以追溯到结果文件或图表数据。

## 10. Final Gate

调用 `08_verification/Final_Gate.md`。

只有以下项目全部通过，才可以报告“完成”：

- 数据审计通过
- 模型逻辑通过
- 代码实际运行
- 关键数字可追溯
- 验证充分
- 图表与结果一致
- 每个子问题都有直接答案
- 论文与代码一致
- 无编造数据/参数/结果

## 失败策略

如果某一步无法完成，不得用假结果补位。应明确输出：

`当前状态：待补充数据 / 待运行 / 待验证 / 待人工确认`

并继续完成不依赖该步骤的工作。
