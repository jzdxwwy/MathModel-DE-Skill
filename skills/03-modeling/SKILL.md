# Stage 03 — Modeling Skill

## 目的
基于 `ProblemMap` 与 `DataProfile` 形成可解释、可验证的模型方案。

## 核心任务
- 候选模型生成
- baseline 建立
- 候选模型比较
- 升级理由
- 假设、变量、参数、目标/方程、约束
- 求解与验证方案

## 输出
- `ModelPlan`
- `ModelSpec`

## Gate
不得因为“模型更高级”而选择模型；必须有数据、问题结构或性能上的明确理由。
